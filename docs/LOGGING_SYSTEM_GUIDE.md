# 로그 및 스크린샷 시스템 가이드

> AdsPower YouTube Premium Automation 프로젝트의 로깅 시스템 설명서

---

## 목차

1. [개요](#1-개요)
2. [폴더 구조](#2-폴더-구조)
3. [세션 로그 (SessionLogService)](#3-세션-로그-sessionlogservice)
4. [터미널 로그 (TerminalLogger)](#4-터미널-로그-terminallogger)
5. [스크린샷 캡처 시점](#5-스크린샷-캡처-시점)
6. [로그 관리 명령어](#6-로그-관리-명령어)
7. [문제 해결](#7-문제-해결)

---

## 1. 개요

이 프로젝트는 **2가지 독립적인 로깅 시스템**을 사용합니다:

| 시스템 | 용도 | 파일 형식 | 저장 위치 |
|--------|------|-----------|-----------|
| **SessionLogService** | 작업별 스크린샷 + 로그 | PNG + log.txt + meta.json | `logs/screenshots/` |
| **TerminalLogger** | 전체 터미널 출력 기록 | JSON | `logs/terminal/` |

```
logs/
├── screenshots/          ← 작업별 스크린샷 + 상세 로그 (SessionLogService)
│   └── 2025-12-28/
│       └── john@gmail.com/
│           └── 09-30-15_pause/
│               ├── 01_navigation.png
│               ├── 02_manage_clicked.png
│               ├── 03_success.png
│               ├── log.txt           ← 사람이 읽는 로그
│               └── meta.json         ← 프로그램용 메타데이터
│
└── terminal/             ← 터미널 전체 출력 (TerminalLogger)
    └── terminal-log-2025-12-28T09-30-15-abc123.json
```

---

## 2. 폴더 구조

### 2.1 세션 로그 폴더 구조

```
logs/screenshots/
└── {YYYY-MM-DD}/                    ← 날짜
    └── {email}/                     ← 계정 이메일
        └── {HH-MM-SS}_{action}/     ← 시작시간_작업종류
            ├── {step}.png           ← 스크린샷들
            ├── log.txt              ← 작업 로그
            └── meta.json            ← 메타데이터
```

**예시:**
```
logs/screenshots/
└── 2025-12-28/
    ├── john@gmail.com/
    │   ├── 09-30-15_pause/          ← 09:30:15에 시작한 일시중지 작업
    │   │   ├── 01_navigation.png
    │   │   ├── 02_manage_clicked.png
    │   │   ├── 03_pause_confirm.png
    │   │   ├── 04_success.png
    │   │   ├── log.txt
    │   │   └── meta.json
    │   └── 14-22-08_resume/         ← 14:22:08에 시작한 결제재개 작업
    │       ├── 01_navigation.png
    │       ├── error_timeout.png
    │       ├── log.txt
    │       └── meta.json
    └── jane@gmail.com/
        └── 10-15-30_pause/
```

### 2.2 action 종류

| action | 설명 |
|--------|------|
| `pause` | 구독 일시중지 |
| `resume` | 구독 결제재개 |

---

## 3. 세션 로그 (SessionLogService)

### 3.1 역할

**작업 단위 로그 관리**: 하나의 일시중지/재개 작업에 대한 모든 정보를 한 폴더에 저장

### 3.2 핵심 메서드

```javascript
// 세션 시작 - 폴더 생성
sessionLogService.startSession('john@gmail.com', 'pause', { profileId: 'k8dj2nf1' });

// 스크린샷 촬영
await sessionLogService.capture(page, '01_navigation', '페이지 이동 완료');

// 세션 종료 - 결과 저장
sessionLogService.endSession('success', { nextBillingDate: '2025-02-15' });
```

### 3.3 log.txt 형식

```
════════════════════════════════════════════════════════
📋 작업 세션: 일시중지 | john@gmail.com
   시작: 2025. 12. 28. 오전 9:30:15
════════════════════════════════════════════════════════

[09:30:15]  세션 시작
[09:30:18] 📸 01_navigation.png - 페이지 이동 완료
[09:30:22] 📸 02_manage_clicked.png - Manage 버튼 클릭
[09:30:28] 📸 03_pause_confirm.png - 일시중지 확인
[09:30:32] 📸 04_success.png - 일시중지 완료
[09:30:32]  작업 완료: success

════════════════════════════════════════════════════════
✅ 작업 완료: success
   종료: 2025. 12. 28. 오전 9:30:32
   소요: 17초
   다음 결제일: 2025-02-15
════════════════════════════════════════════════════════
```

### 3.4 meta.json 형식

```json
{
  "session": {
    "id": "20251228-093015-pause",
    "email": "john@gmail.com",
    "action": "pause",
    "startTime": "2025-12-28T00:30:15.123Z",
    "endTime": "2025-12-28T00:30:32.456Z",
    "duration": 17
  },
  "result": {
    "status": "success",
    "nextBillingDate": "2025-02-15",
    "error": null
  },
  "environment": {
    "language": "en",
    "profileId": "k8dj2nf1"
  },
  "screenshots": [
    { "file": "01_navigation.png", "step": "navigation", "time": "09:30:18" },
    { "file": "02_manage_clicked.png", "step": "manage_clicked", "time": "09:30:22" },
    { "file": "03_pause_confirm.png", "step": "pause_confirm", "time": "09:30:28" },
    { "file": "04_success.png", "step": "success", "time": "09:30:32" }
  ],
  "logs": [
    { "time": "09:30:15", "message": "세션 시작" },
    { "time": "09:30:32", "message": "작업 완료: success" }
  ]
}
```

### 3.5 스크린샷 파일명 규칙

| 접두사 | 설명 | 예시 |
|--------|------|------|
| `01_`, `02_`, ... | 정상 진행 단계 | `01_navigation.png` |
| `error_` | 에러 발생 시점 | `error_timeout.png` |
| `debug_` | 디버그용 추가 캡처 | `debug_popup.png` |

---

## 4. 터미널 로그 (TerminalLogger)

### 4.1 역할

**전체 터미널 출력 캡처**: `console.log`, `console.error` 등 모든 출력을 JSON으로 저장

### 4.2 작동 방식

```
┌─────────────────────────────────────────────────────────┐
│  코드에서 console.log('Hello')                          │
│          ↓                                              │
│  TerminalLogger가 인터셉트                               │
│          ↓                                              │
│  1. 원래 터미널에 출력 (사용자 보임)                     │
│  2. JSON 버퍼에 저장                                    │
│          ↓                                              │
│  버퍼가 10개 차면 → 파일에 기록                         │
│  또는 5초마다 → 파일에 기록                             │
└─────────────────────────────────────────────────────────┘
```

### 4.3 초기화

```javascript
// index.js에서 초기화
const { initializeTerminalLogger } = require('./src/infrastructure/logging/TerminalLogger');

async function main() {
  await initializeTerminalLogger();  // 터미널 로거 시작
  // ... 이후 모든 console.log가 자동 캡처됨
}
```

### 4.4 로그 파일 형식

**파일명**: `terminal-log-{timestamp}-{sessionId}.json`

```json
{
  "version": "2.0.0",
  "sessionId": "abc123xyz",
  "startTime": "2025-12-28T00:30:15.000Z",
  "pid": 12345,
  "platform": "win32",
  "nodeVersion": "v18.17.0",
  "cwd": "C:/Users/admin/SF",
  "env": {
    "USE_MOCK_REPOSITORY": "false",
    "DEBUG_MODE": "false",
    "NODE_ENV": "production"
  },
  "logs": [
    {
      "timestamp": "2025-12-28T00:30:16.123Z",
      "level": "log",
      "message": "[09:30] 🚀 통합워커 시작",
      "raw": ["[09:30] 🚀 통합워커 시작"]
    },
    {
      "timestamp": "2025-12-28T00:30:18.456Z",
      "level": "info",
      "message": "[09:30] 📋 작업 발견: 재개 2건",
      "raw": ["[09:30] 📋 작업 발견: 재개 2건"]
    }
  ],
  "lastUpdate": "2025-12-28T00:45:30.789Z"
}
```

### 4.5 특수 처리

| 처리 | 설명 |
|------|------|
| ANSI 코드 제거 | 터미널 색상 코드 (`\x1b[32m`) 제거 |
| 순환 참조 처리 | 객체 순환 참조 시 `[Circular Reference]` 표시 |
| 긴 문자열 자르기 | 10,000자 초과 시 잘라서 저장 |
| Windows 경로 정규화 | 백슬래시(`\`) → 슬래시(`/`) 변환 |

---

## 5. 스크린샷 캡처 시점

### 5.1 일시중지 작업 (EnhancedPauseSubscriptionUseCase)

```
작업 시작
    │
    ├─ [Step 1] 페이지 이동 ─────────────┐
    │                                    │
    ├─ [Step 2] 언어 감지                │
    │                                    │
    ├─ [Step 3] Manage 버튼 클릭 ────────┼─→ 📸 02_manage_clicked.png
    │                                    │
    ├─ [Step 4] Deactivate 클릭          │
    │                                    │
    ├─ [Step 5] 일시중지 확인 클릭 ──────┼─→ 📸 03_pause_confirm.png
    │                                    │
    ├─ [Step 6] 완료 확인 ───────────────┼─→ 📸 04_success.png
    │                                    │
    └─ 세션 종료                         │
                                         │
    ⚠️ 오류 발생시 ──────────────────────┼─→ 📸 error_{type}.png
                                         │
    ⚠️ 만료 감지시 (Manage 확장) ────────┼─→ 📸 expired_subscription.png
```

### 5.2 결제재개 작업 (EnhancedResumeSubscriptionUseCase)

```
작업 시작
    │
    ├─ [Step 1] 페이지 이동
    │
    ├─ [Step 2] 언어 감지
    │
    ├─ [Step 3] Manage 버튼 클릭 ────────→ 📸 02_manage_clicked.png
    │
    ├─ [Step 4] Resume 버튼 클릭
    │
    ├─ [Step 5] 확인 팝업 처리 ──────────→ 📸 03_resume_confirm.png
    │
    ├─ [Step 6] 완료 확인 ───────────────→ 📸 04_success.png
    │
    └─ 세션 종료

    ⚠️ 오류 발생시 ──────────────────────→ 📸 error_{type}.png
```

### 5.3 중요: Manage 버튼 확장 후 스크린샷

**v2.12부터** 스크린샷은 Manage 버튼이 클릭되어 확장 영역이 보이는 상태에서 촬영됩니다:

```javascript
// 스크린샷 촬영 전 확장 영역 열기
await this.clickManageButton();      // Manage 버튼 클릭
await new Promise(r => setTimeout(r, 2000));  // 애니메이션 대기
await sessionLogService.capture(page, '04_success', '일시중지 완료');
```

이렇게 하면 스크린샷에서 구독 상태, 다음 결제일 등 중요 정보를 확인할 수 있습니다.

---

## 6. 로그 관리 명령어

### 6.1 세션 통계 확인

```bash
npm run logs:stats
# 또는
node scripts/session-stats.js
```

**출력 예시:**
```
📊 세션 로그 통계
════════════════════════════════════════

날짜별 통계:
─────────────────────────────────────────
2025-12-28: ✅12 ❌3 (스크린샷: 45개, 1.2 MB)
2025-12-27: ✅8 ❌1 (스크린샷: 28개, 0.8 MB)
─────────────────────────────────────────
합계: ✅20 ❌4 (스크린샷: 73개, 2.0 MB)
```

### 6.2 오래된 로그 정리

```bash
# 3일 이전 로그 삭제 (기본값)
npm run logs:cleanup

# 7일 이전 로그 삭제
npm run logs:cleanup -- --days 7
```

### 6.3 전체 로그 삭제

```bash
npm run logs:clear
# 또는
node scripts/session-clear.js
```

### 6.4 자동 정리

**SessionLogService**는 기본적으로 **3일 이전** 로그를 자동 삭제합니다:

```javascript
// 서비스 내부에서 자동 호출
await sessionLogService.cleanup(3);  // 3일 이전 삭제
```

---

## 7. 문제 해결

### 7.1 스크린샷이 저장되지 않음

**원인**: 세션이 시작되지 않음

```javascript
// ❌ 잘못된 예 - 세션 없이 캡처 시도
await sessionLogService.capture(page, 'step1');  // 무시됨

// ✅ 올바른 예
sessionLogService.startSession(email, 'pause');  // 먼저 세션 시작
await sessionLogService.capture(page, 'step1');  // 정상 저장
```

### 7.2 터미널 로그가 비어 있음

**원인**: 초기화 누락

```javascript
// index.js에서 반드시 호출
const { initializeTerminalLogger } = require('./src/infrastructure/logging/TerminalLogger');

async function main() {
  await initializeTerminalLogger();  // ← 이 줄 확인
  // ...
}
```

### 7.3 JSON 파싱 오류

**원인**: 특수 문자나 ANSI 코드

**해결**: TerminalLogger v2.0에서 자동 정제됨:
- ANSI 색상 코드 제거
- 제어 문자 제거
- 순환 참조 처리

### 7.4 디스크 공간 부족

**해결**:
```bash
# 전체 로그 삭제
npm run logs:clear

# 또는 수동 삭제
rm -rf logs/screenshots/*
rm -rf logs/terminal/*
```

---

## 부록: 관련 파일

| 파일 | 역할 |
|------|------|
| `src/services/SessionLogService.js` | 세션 로그 서비스 |
| `src/infrastructure/logging/TerminalLogger.js` | 터미널 로그 서비스 |
| `scripts/session-stats.js` | 통계 조회 스크립트 |
| `scripts/session-clear.js` | 전체 삭제 스크립트 |
| `scripts/session-cleanup.js` | 오래된 로그 정리 스크립트 |

---

*문서 버전: 1.0 | 최종 수정: 2025-12-28*
