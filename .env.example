# ==============================================
# AdsPower YouTube Automation 환경 변수 설정
# ==============================================
# 이 파일을 .env로 복사하고 실제 값을 입력하세요
# cp .env.example .env
# ==============================================

# 1. AdsPower API 설정
# ==============================================
# AdsPower API URL (기본값: http://127.0.0.1:50325)
# ⚠️ 주의: AdsPower 업데이트 후 포트가 변경될 수 있습니다!
# 일반적인 포트: 50325, 50326, 50327
#
# 💡 자동 포트 감지 기능:
# - 연결 실패시 50325~50327 포트를 자동으로 순차 시도합니다
# - 작동하는 포트를 찾으면 자동으로 사용합니다
# - 콘솔에서 올바른 포트를 확인하고 아래 값을 업데이트하면 더 빠릅니다
#
# 포트 확인 방법:
# Windows: netstat -an | findstr "LISTENING" | findstr ":50"
# Mac/Linux: netstat -an | grep LISTEN | grep ":50"
ADSPOWER_API_URL=http://127.0.0.1:50325

# AdsPower 설치 경로 (선택사항)
# Windows 예시: C:\Program Files\AdsPower Global
# Mac 예시: /Applications/AdsPower.app
ADSPOWER_PATH=

# 2. Google Sheets 설정
# ==============================================
# Google Sheets ID (URL에서 /d/ 다음 부분)
# 예시: https://docs.google.com/spreadsheets/d/SHEET_ID_HERE/edit
GOOGLE_SHEETS_ID=your_google_sheets_id_here

# Google Service Account 인증 파일 경로
# 상대 경로 또는 절대 경로 사용 가능
GOOGLE_SERVICE_ACCOUNT_PATH=./credentials/service-account.json

# Google Sheets 탭 이름 (선택사항, 기본값 사용 권장)
SHEET_TAB_NAME=Sheet1
PAUSE_SHEET_TAB_NAME=일시정지
RESUME_SHEET_TAB_NAME=재개

# 3. 워크플로우 설정
# ==============================================
# 디버그 모드 (true/false)
DEBUG_MODE=false

# 스텔스 모드 (true/false) - 브라우저 자동화 감지 방지
STEALTH_MODE=true

# 배치 크기 (동시 처리할 프로필 수)
BATCH_SIZE=5

# 대기 시간 설정 (밀리초)
DEFAULT_WAIT_TIME=3000
NAVIGATION_TIMEOUT=30000
PAGE_LOAD_TIMEOUT=60000

# 재시도 설정
MAX_RETRIES=3
RETRY_DELAY=5000

# 4. 로깅 설정
# ==============================================
# 로그 레벨 (error, warn, info, debug)
LOG_LEVEL=info

# 로그 파일 경로
LOG_FILE_PATH=./logs

# 로그 파일 보존 기간 (일)
LOG_RETENTION_DAYS=30

# 터미널 로깅 설정 (모든 console 출력을 JSON 파일로 저장)
ENABLE_TERMINAL_LOGGING=true

# 터미널 로그 보존 시간 (밀리초, 기본값: 48시간 = 172800000)
TERMINAL_LOG_MAX_AGE=172800000

# 터미널 로그 통계 표시 여부
SHOW_LOG_STATS=false

# 5. 보안 설정 (선택사항)
# ==============================================
# API 키 (필요한 경우)
API_KEY=

# 암호화 키 (민감한 데이터 암호화용)
ENCRYPTION_KEY=

# 6. Anti-Captcha 설정 (선택사항)
# ==============================================
# Anti-Captcha API 키 (이미지 CAPTCHA 자동 해결)
# https://anti-captcha.com 에서 가입 후 API 키 발급
# 비용: 약 $3 / 1000건 (ImageToTextTask)
#
# 💡 사용법:
# - API 키가 설정되면 이미지 CAPTCHA 자동 해결 시도
# - API 키가 없으면 수동 해결 모드로 동작 (60초 대기)
# - 자동 해결 실패 시에도 수동 해결 모드로 전환 (안전망)
#
# 지원 CAPTCHA 유형:
# - Google 로그인 이미지 CAPTCHA ("들리거나 표시된 텍스트 입력")
# - 일반 텍스트 기반 이미지 CAPTCHA
ANTI_CAPTCHA_API_KEY=

# 7. 프록시 설정 (선택사항)
# ==============================================
# HTTP 프록시
HTTP_PROXY=

# HTTPS 프록시
HTTPS_PROXY=

# 프록시 제외 대상
NO_PROXY=localhost,127.0.0.1

# 8. 고급 설정
# ==============================================
# 언어 설정 (ko, en, ja, zh, vi, th, id, ms, pt, es, de, fr, ru, ar, hi)
DEFAULT_LANGUAGE=ko

# 스크린샷 저장 여부
SAVE_SCREENSHOTS=true
SCREENSHOT_PATH=./screenshots

# 세션 유지 모드
KEEP_SESSION_ALIVE=true

# 브라우저 헤드리스 모드 (true = 백그라운드 실행)
HEADLESS_MODE=false

# 작업 완료 후 자동 종료 (true = 작업 완료시 프로그램 종료)
# false로 설정하면 작업 후 계속 여부를 묻습니다
AUTO_EXIT_AFTER_TASK=false

# 9. 알림 설정 (선택사항)
# ==============================================
# Slack Webhook URL
SLACK_WEBHOOK_URL=

# Discord Webhook URL
DISCORD_WEBHOOK_URL=

# Email 알림 설정
EMAIL_NOTIFICATIONS=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
NOTIFICATION_EMAIL=

# ==============================================
# 중요: 이 파일의 실제 .env 파일은 절대 Git에 커밋하지 마세요!
# ==============================================